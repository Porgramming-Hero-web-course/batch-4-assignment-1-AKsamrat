{
  //

  const getProperty = <s, n extends keyof s>(obj: s, name: n) => {
    return obj[name];
  };
  // const person = { name: 'Alice', age: 30 };
  // console.log(getProperty(person, 'age'));

  //
}
